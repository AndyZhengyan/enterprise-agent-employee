# conftest.py — minimal pytest configuration for e-agent-os
# The package is installed in editable mode via `pip install -e ".[dev]"`,
# which makes `apps` and `common` importable without needing sys.path tricks.
